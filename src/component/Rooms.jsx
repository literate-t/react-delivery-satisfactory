import styled from "@emotion/styled";

const Body = styled.div`
  flex-basis: 0;
  flex-grow: 999;
  padding: 1rem;
  background-color: var(--primary);
`;

const Rooms = () => {
    return <Body>Rooms</Body>
}

export default Rooms;